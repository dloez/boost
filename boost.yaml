vars:
  - pypi_username: VALUE
  - pypi_password: VALUE
    attributes: secret 
boost:
  lint: poetry run pylint --rcfile pyproject.toml boostbuild
  publish: poetry publish --build --username {pypi_username} --password {pypi_password}